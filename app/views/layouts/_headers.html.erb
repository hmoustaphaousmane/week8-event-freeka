<nav class="navbar navbar navbar-expand navbar-dark bg-primary sticky-top">
  <div class="container-fluid">
    <%= link_to "EventFreeka", "#", :class => "navbar-brand"%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample02" aria-controls="navbarsExample02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarsExample02">
      <% if user_signed_in? %>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to "New Event", "#", :class => "nav-link"%>
          </li>
          <li class="nav-item">
            <%= link_to "Link", "#", :class => "nav-link"%>
          </li>
          <li>
            <%= form_tag destroy_user_session_path, method: :delete, class: 'nav-link nav navbar-right' do %>
            <%= submit_tag 'Log out' %>
            <% end %>
          </li>
        </ul>

        </div class="col-12 d-flex justify-content-end mb-2">
        <div class="dropdown">
          <button type="button" class="btn btn-info dropdown-toggle" data-toggle="dropdown">
            <span class="caret"></span>
            <span class="sr-only">Profile</span>
          </button>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to "View Profile", user_path(current_user.id) %></li>
            <li>
              <%= form_tag destroy_user_session_path, method: :delete, class: 'nav-link nav navbar-right' do %>
              <%= submit_tag 'Log out' %>
              <% end %>
            </li>
          </ul>
        </div>
      <% else %>
      </div class="col-12 d-flex justify-content-end mb-2">
      <div class="dropdown">
        <button type="button" class="btn btn-info dropdown-toggle" data-toggle="dropdown">
          <span class="caret"></span>
          <span class="sr-only">Sign In/Sign Up</span>
        </button>
        <ul class="dropdown-menu" role="menu">
          <li><%= link_to "Log In", new_user_session_path %></li>
          <li><%= link_to "Sign Up", new_user_registration_path %></li>
        </ul>
      </div>
      </div>
      <% end %>
  </div>
</nav>
